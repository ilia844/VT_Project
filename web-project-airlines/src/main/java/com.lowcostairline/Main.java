package com.lowcostairline;

import org.xml.sax.SAXException;

import javax.xml.parsers.ParserConfigurationException;
import java.io.IOException;

public class Main {


    public static void main(String[] args) throws ParserConfigurationException, SAXException, IOException {
        //Controller controller = new Controller();
       // controller.writeToFile();
        /*
        SAXParserFactory factory = SAXParserFactory.newInstance();
        SAXParser parser = factory.newSAXParser();

        SaxParser handler = new SaxParser();
        parser.parse(new File("aircraft.xml"), handler);
        ArrayList<Aircraft> aircrafts = handler.getAircrafts();

        for (Aircraft aircraft : aircrafts)
            System.out.println(String.format("Tank capacity: %d, model: %s", aircraft.getTankCapacity(), aircraft.getModel()));
        */
    }
}
